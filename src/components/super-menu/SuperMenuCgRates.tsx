"use client";

type SuperMenuCgRatesProps = {
  menuOpen: boolean;
};

/** Hidden until a live commodity API is wired — no mock rates in reader UI */
export function SuperMenuCgRates(_props: SuperMenuCgRatesProps) {
  return null;
}
